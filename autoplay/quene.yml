info:
  name: quene
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8000/api/queue/:playlist
  params:
    - name: playlist
      value: test2
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
