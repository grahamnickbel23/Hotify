info:
  name: stream
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8000/api/hotify/stream/:playlist/:track
  params:
    - name: playlist
      value: test
      type: path
    - name: track
      value: Bulbuli.opus
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
