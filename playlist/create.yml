info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: http://100.123.126.113:8001/playlist/create
  body:
    type: json
    data: |-
      {
          "name" : "test-02"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
