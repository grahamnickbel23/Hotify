info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/playlists
  body:
    type: json
    data: "{\r\n    \"name\" : \"test-02\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
